<h2>Create a new bookmark</h2>
<!-- ici on a besoin de passer deux modèles pour générer la bonne route -->
<!-- pour vérifier la route de ton form regarde dans l'inspecteur du navigateur l'action de ton form -->

<%= simple_form_for([@list, @bookmark]) do |f| %>
<%= f.label :comment %>
<%= f.text_field :comment %>
<%= f.label :movie %>
<%= f.association :movie %>
<%= f.submit %>
<% end %>
